Gem::Specification.new do |s|
  s.name        = 'loft-harmony'
  s.version     = '1.1.0'
  s.date        = '2014-07-10'
  s.summary     = 'Watches a directory for changes to upload to a server via FTP'
  s.description = 'Watches a directory for changes to upload to a server via FTP'
  s.authors     = ['Caleb Simpson']
  s.email       = 'caleb@simpson.center'
  s.files       = []
  s.executables = ['harmony']
end
